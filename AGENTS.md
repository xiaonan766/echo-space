# AI 生成代码规范

本规范适用于当前前端工作区 `echo-space-frontend`，包括：

- `echo-space-frontend-admin`
- `echo-space-frontend-web`

AI 在本项目中生成、修改或解释代码时，必须优先遵守本文件。

## 核心原则

- 优先保证代码可读性、结构清晰、易维护。
- 生成的代码要适合写入简历和面试讲解：思路清楚、命名明确、职责边界容易说明。
- 选择简单、稳定、容易解释的实现，不要为了炫技引入复杂架构。
- 不要一次性重构无关代码。
- 不要删除已有功能，除非用户明确要求。
- 不要引入没有必要的新依赖。
- 不要生成无法运行的伪代码。
- 不要只给思路，必须给出可落地的代码。

## 技术栈约束

项目已经存在实际技术栈，必须以现有配置为准。

- 构建工具：Vite
- 前端框架：Vue 3
- 组件写法：优先遵循现有 `.vue` 单文件组件和 `<script setup>` 风格
- 路由：Vue Router
- 状态管理：Pinia
- UI 组件库：Element Plus
- 样式：SCSS / Sass
- 请求：Axios，优先复用 `src/utils/Request.js`
- 工具函数：优先复用 `src/utils` 下已有工具
- 路径别名：优先使用项目已有 `@` 别名

禁止在没有明确需求的情况下更换技术栈，例如：

- 不要把 Vue 项目改成 React、Next、Nuxt 或其他框架。
- 不要在当前 JavaScript 项目中擅自引入 TypeScript。
- 不要替换 Element Plus 为其他 UI 库。
- 不要绕开现有 `utils`、`router`、`components` 的组织方式重新设计一套体系。

## 编码与中文文本

- 所有新增和修改的文本内容必须保持正常可读，不能生成中文乱码，也不能生成一串问号。
- 文件保存时应使用 UTF-8 编码。
- 不要复制已有乱码作为新文案，例如 `鍙栨秷`、`椤甸潰` 这类内容。
- 如果修改的代码附近存在中文乱码，只修复和本次需求直接相关的文案，不要顺手大面积改动无关文件。
- 中文按钮、提示、表单校验、空状态文案必须写成真实语义，例如 `取消`、`确认`、`暂无数据`。
- 如果不确定中文文案，优先使用简洁、通用、不会误导用户的表达。

## 修改代码前的检查

开始写代码前，必须先理解现有目录结构和代码风格：

- 阅读目标子项目的 `package.json`、`vite.config.js`、`jsconfig.json`。
- 查看 `src` 下已有目录，包括 `components`、`views`、`router`、`utils`、`assets`。
- 新增页面前先查看相邻页面的写法。
- 新增组件前先查看 `src/components` 是否已有可复用组件。
- 新增接口调用前先查看 `src/utils/Api.js` 和 `src/utils/Request.js`。
- 新增状态管理前先确认是否已有 Pinia store 可复用，且只有跨页面共享状态才放入 store。

## 目录与复用规则

- 页面级代码放在 `src/views` 中，遵循现有业务目录组织。
- 通用组件放在 `src/components` 中；只在单个页面使用的组件可放在页面附近或按现有风格处理。
- 通用函数放在 `src/utils` 中，避免在多个页面复制粘贴相同逻辑。
- 路由配置放在 `src/router` 中。
- 静态资源放在 `src/assets` 中。
- 新增功能时应尽量复用已有组件、工具函数、请求封装和类型约定。
- 不要为了一个很小的功能新增多层目录、多层抽象或复杂设计模式。

## 命名规范

- 命名必须清晰表达业务含义。
- 禁止使用 `foo`、`bar`、`temp`、`data1`、`test1`、`aaa` 这类无意义命名。
- 变量名应说明它存放的内容，例如 `videoList`、`uploadProgress`、`selectedCategoryId`。
- 方法名应说明动作，例如 `loadVideoList`、`handleSubmit`、`resetSearchForm`。
- 组件名使用清晰的 PascalCase，例如 `VideoUploadDialog.vue`。
- 布尔值命名优先使用 `is`、`has`、`can`、`show` 等前缀，例如 `isLoading`、`hasMore`、`showDialog`。

## Vue 代码规范

- 优先使用现有项目的 Vue 3 `<script setup>` 风格。
- 组件职责要单一，页面负责组织业务流程，通用组件负责展示和交互。
- 表单、弹窗、列表、分页、上传等能力优先复用项目已有组件。
- 事件处理函数使用 `handleXxx` 命名，例如 `handlePageChange`、`handleUploadSuccess`。
- 接口加载函数使用 `loadXxx` 或 `getXxx` 命名，例如 `loadUserList`。
- 避免在模板中写复杂表达式，复杂逻辑应放入计算属性或方法中。
- 避免过深的模板嵌套，必要时拆分为小组件。
- 只在确实需要全局复用时注册全局组件，否则优先局部使用。

## 样式规范

- 优先沿用已有 SCSS 写法和页面布局风格。
- 不要引入新的 CSS 框架。
- 样式命名应和组件或业务含义一致。
- 页面样式尽量控制在当前组件内，避免污染全局样式。
- 修改公共样式前要确认影响范围。
- 不要通过大量 `!important` 解决局部问题，除非项目已有组件覆盖确实需要。

## 接口与数据处理

- 请求逻辑优先使用现有 `Request.js` 封装。
- 接口地址优先维护在已有 `Api.js` 中。
- 不要在页面中硬编码大量接口路径。
- 接口返回数据要做必要的空值保护，避免页面直接报错。
- 列表数据、分页数据、表单数据应保持结构清晰，字段命名贴近业务。
- 不要伪造接口或写无法运行的 mock 逻辑，除非用户明确要求。

## 依赖管理

- 新增依赖前必须先判断现有依赖是否已经能完成需求。
- 小功能优先用原生能力、Vue、Element Plus 或已有工具函数实现。
- 确实需要新增依赖时，必须说明原因、用途和替代方案。
- 不要因为个人偏好引入新的状态库、请求库、日期库、图标库或 UI 库。
- 不要手动修改 `node_modules`。

## 改动边界

- 只修改和当前需求直接相关的文件。
- 不要格式化整个项目或无关文件。
- 不要清理、删除、重命名已有功能，除非用户明确要求。
- 遇到已有代码问题时，如果不影响当前需求，可在说明中指出，不要擅自大范围重构。
- 如果必须调整公共组件或公共工具函数，要同步检查调用方，避免破坏现有功能。

## 可运行要求

- 生成的代码必须能在当前项目中落地运行。
- 修改完成后，优先在受影响子项目中运行 `npm run build` 验证。
- 如果没有运行验证，必须说明原因。
- 不要留下未定义变量、未导入组件、未使用依赖、明显语法错误。
- 不要提交只有注释、只有 TODO、只有伪代码的实现。

## AI 输出要求

- 回答用户时要说明改了哪些文件、实现了什么效果、如何验证。
- 如果用户要求直接改代码，不要只给方案，应完成实际文件修改。
- 如果需求不清晰，可以先做最合理的保守实现；存在高风险歧义时再提问。
- 对复杂需求，应先梳理现有代码，再给出简洁计划，然后实现。
- 对简历或面试导向的功能，应在实现中保持层次清楚，并避免不必要的复杂度。

## 推荐工作流程

1. 确认要修改的是 `echo-space-frontend-admin` 还是 `echo-space-frontend-web`。
2. 阅读对应子项目的配置、入口文件、相关页面、相关组件和工具函数。
3. 选择最小可行且符合现有风格的实现方案。
4. 编写可运行代码，优先复用已有能力。
5. 检查中文文案和编码，避免乱码或问号。
6. 运行构建或必要验证。
7. 简洁说明改动结果和验证结果。
